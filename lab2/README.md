# Система электронного документооборота (СЭД)

## Статистика проекта

- **Всего классов:** 43
- **Классов исключений:** 12
- **Протоколов (интерфейсов):** 4
- **Всего полей:** 116
- **Всего методов:** 60
- **Ассоциаций между классами:** 38

## Описание проекта

Система электронного документооборота (СЭД) - это комплексное решение для управления документами в организации. Система поддерживает:

- Создание и хранение различных типов документов
- Маршруты согласования документов
- Электронную подпись документов
- Управление версиями документов
- Платежные операции и счета
- Систему безопасности и аутентификации
- Уведомления пользователей

## Архитектура

Проект организован в модули по функциональному назначению:
- `core.py` - базовые сущности и протоколы
- `exceptions.py` - исключения
- `users.py` - пользователи, роли и организации
- `documents.py` - типы документов
- `workflow.py` - маршруты согласования
- `storage.py` - хранилище документов
- `security.py` - безопасность и аутентификация
- `payments.py` - платежная система
- `services.py` - бизнес-логика и сервисы
- `cli.py` - демонстрационное приложение

---

## Модуль: core.py

### Класс `BaseEntity`

**Описание:** Базовый класс для всех сущностей системы с автоматическими временными метками.

**Поля:**
- `created_at: datetime` - дата и время создания (автоматически)
- `updated_at: datetime` - дата и время последнего обновления (автоматически)

**Методы:**
- `touch() -> None` - обновляет временную метку `updated_at`

**Декоратор:** `@dataclass(kw_only=True)`

---

### Класс `IdentifiableMixin`

**Описание:** Миксин для классов с идентификатором.

**Поля:**
- `id: str` - уникальный идентификатор

**Методы:**
- `identity() -> str` - возвращает идентификатор объекта

---

### Класс `TimestampMixin`

**Описание:** Миксин для классов с временными метками.

**Поля:**
- `created_at: datetime` - дата создания
- `updated_at: datetime` - дата последнего обновления

---

### Класс `Validatable` (ABC)

**Описание:** Абстрактный класс для объектов, поддерживающих валидацию.

**Методы:**
- `validate() -> None` - выполняет валидацию объекта

---

### Класс `Approvable` (ABC)

**Описание:** Абстрактный класс для объектов, которые можно согласовать.

**Методы:**
- `approve() -> None` - согласовывает объект

---

### Класс `Signable` (ABC)

**Описание:** Абстрактный класс для объектов, которые можно подписать.

**Методы:**
- `sign(user_id: str) -> None` - подписывает объект указанным пользователем

---

### Класс `Storable` (ABC)

**Описание:** Абстрактный класс для объектов, которые можно сохранить.

**Методы:**
- `store() -> str` - сохраняет объект и возвращает идентификатор

---

### Протокол `NotifierProtocol`

**Описание:** Протокол для отправки уведомлений.

**Методы:**
- `notify(message: str) -> None` - отправляет уведомление

**Декоратор:** `@runtime_checkable`

---

### Протокол `PaymentProcessorProtocol`

**Описание:** Протокол для обработки платежей.

**Методы:**
- `transfer(src_account: str, dst_account: str, amount: int) -> str` - переводит средства между счетами

**Декоратор:** `@runtime_checkable`

---

### Протокол `DocumentLike`

**Описание:** Протокол для документоподобных объектов.

**Поля:**
- `number: str` - номер документа
- `title: str` - название документа

**Методы:**
- `validate() -> None` - валидирует документ

---

### Протокол `DocumentRepositoryProtocol`

**Описание:** Протокол для репозитория документов.

**Методы:**
- `save(doc: DocumentLike) -> None` - сохраняет документ
- `get(number: str) -> Optional[DocumentLike]` - получает документ по номеру
- `exists(number: str) -> bool` - проверяет существование документа
- `search(query: str) -> Iterable[DocumentLike]` - поиск документов

**Декоратор:** `@runtime_checkable`

---

## Модуль: users.py

### Класс `Permission`

**Описание:** Представляет разрешение в системе.

**Поля:**
- `code: str` - код разрешения
- `description: str` - описание разрешения (по умолчанию "")

**Декоратор:** `@dataclass(frozen=True)`

**Ассоциации:**
- Используется в классе `Role` (поле `permissions`)

---

### Класс `Role`

**Описание:** Роль пользователя с набором разрешений.

**Поля:**
- `name: str` - название роли
- `permissions: Set[Permission]` - набор разрешений

**Методы:**
- `allows(code: str) -> bool` - проверяет, есть ли разрешение с указанным кодом

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит множество объектов `Permission`
- Используется в классе `User` (поле `roles`)

---

### Класс `Department`

**Описание:** Отдел/подразделение организации.

**Поля:**
- `name: str` - название отдела
- `cost_center: str` - центр затрат

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `User` (поле `department`)
- Используется в классе `Document` (поле `department`)

---

### Класс `AccessPolicy`

**Описание:** Политика доступа на основе ролей.

**Поля:**
- `allowed_roles: Set[str]` - набор разрешенных ролей

**Методы:**
- `can_access(roles: List[Role]) -> bool` - проверяет, может ли пользователь с указанными ролями получить доступ

**Декоратор:** `@dataclass`

---

### Класс `Organization`

**Описание:** Организация.

**Поля:**
- `name: str` - название организации
- `inn: str` - ИНН (идентификационный номер налогоплательщика)
- `address: str` - адрес (по умолчанию "")

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `User` (поле `org`)
- Используется в классе `Document` (поле `organization`)

---

### Класс `User`

**Описание:** Пользователь системы.

**Поля:**
- `id: str` - уникальный идентификатор
- `login: str` - логин пользователя
- `display_name: str` - отображаемое имя
- `is_blocked: bool` - признак блокировки (по умолчанию False)
- `roles: List[Role]` - список ролей пользователя
- `department: Department | None` - отдел пользователя
- `org: Organization | None` - организация пользователя

**Методы:**
- `has_permission(code: str) -> bool` - проверяет наличие разрешения по коду
- `assign_role(role: Role) -> None` - назначает роль пользователю

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит список объектов `Role`
- Содержит ссылку на `Department`
- Содержит ссылку на `Organization`
- Используется в классе `Document` (поле `author`)

---

## Модуль: documents.py

### Класс `DocumentMetadata`

**Описание:** Метаданные документа.

**Поля:**
- `tags: List[str]` - теги документа
- `attributes: Dict[str, str]` - произвольные атрибуты

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `Document` (поле `metadata`)

---

### Класс `DocumentAttachment`

**Описание:** Вложение к документу.

**Поля:**
- `filename: str` - имя файла
- `content_type: str` - MIME-тип
- `size: int` - размер в байтах
- `checksum: str` - контрольная сумма

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `Document` (поле `attachments`)
- Используется в классе `DocumentStorage` (метод `store_attachment`)

---

### Класс `DocumentVersion`

**Описание:** Версия документа.

**Поля:**
- `number: int` - номер версии
- `content: str` - содержимое версии
- `created_at: datetime` - дата создания версии
- `author_id: str` - идентификатор автора версии

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `Document` (поле `versions`)

---

### Класс `Signature`

**Описание:** Электронная подпись документа.

**Поля:**
- `user_id: str` - идентификатор подписавшего
- `certificate_id: str` - идентификатор сертификата
- `signed_at: datetime` - дата и время подписания

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `Document` (поле `signatures`)

---

### Класс `DigitalCertificate`

**Описание:** Цифровой сертификат для электронной подписи.

**Поля:**
- `id: str` - идентификатор сертификата
- `subject: str` - субъект сертификата
- `valid_from: datetime` - дата начала действия
- `valid_to: datetime` - дата окончания действия

**Методы:**
- `is_valid(moment: datetime) -> bool` - проверяет, действителен ли сертификат в указанный момент

**Декоратор:** `@dataclass`

---

### Класс `DocumentHistoryRecord`

**Описание:** Запись в истории документа.

**Поля:**
- `event: str` - название события
- `actor_id: str` - идентификатор пользователя
- `occurred_at: datetime` - дата и время события
- `details: str` - детали события (по умолчанию "")

**Декоратор:** `@dataclass`

---

### Класс `DocumentLock`

**Описание:** Блокировка документа для редактирования.

**Поля:**
- `owner_id: str` - идентификатор владельца блокировки
- `acquired_at: datetime` - дата получения блокировки

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `Document` (поле `_lock`)

---

### Класс `Document`

**Описание:** Базовый класс документа.

**Базовые классы:** `IdentifiableMixin`, `Validatable`, `Approvable`, `Signable`, `BaseEntity`

**Поля:**
- `id: str` - уникальный идентификатор
- `number: str` - номер документа
- `title: str` - название документа
- `author: User` - автор документа
- `organization: Organization | None` - организация
- `department: Department | None` - отдел
- `status: str` - статус документа (по умолчанию NEW)
- `versions: List[DocumentVersion]` - версии документа
- `attachments: List[DocumentAttachment]` - вложения
- `approval_route: ApprovalRoute | None` - маршрут согласования
- `metadata: DocumentMetadata` - метаданные
- `_lock: DocumentLock | None` - блокировка
- `signatures: List[Signature]` - электронные подписи

**Методы:**
- `validate() -> None` - валидирует документ (проверяет обязательные поля)
- `add_version(content: str, author_id: str) -> DocumentVersion` - добавляет новую версию
- `add_attachment(attachment: DocumentAttachment) -> None` - добавляет вложение
- `lock(user_id: str) -> None` - блокирует документ для редактирования
- `unlock(user_id: str) -> None` - снимает блокировку
- `approve() -> None` - согласовывает документ
- `sign(user_id: str) -> None` - подписывает документ
- `archive() -> None` - архивирует документ
- `restore() -> None` - восстанавливает документ из архива

**Декоратор:** `@dataclass(kw_only=True)`

**Ассоциации:**
- Содержит ссылку на `User` (автор)
- Содержит ссылку на `Organization`
- Содержит ссылку на `Department`
- Содержит список `DocumentVersion`
- Содержит список `DocumentAttachment`
- Содержит ссылку на `ApprovalRoute`
- Содержит `DocumentMetadata`
- Содержит `DocumentLock`
- Содержит список `Signature`

**Исключения:**
- `ValueError` - при невалидных данных документа
- `VersionConflictError` - при конфликте блокировок
- `InvalidDocumentStatusError` - при недопустимых операциях в текущем статусе
- `InvalidSignatureError` - при попытке подписать документ без версий

---

### Класс `IncomingDocument`

**Описание:** Входящий документ.

**Базовые классы:** `Document`

**Поля:**
- `sender: str` - отправитель (по умолчанию "")

**Методы:**
- `approve() -> None` - согласовывает документ (переопределен)

**Декоратор:** `@dataclass`

---

### Класс `OutgoingDocument`

**Описание:** Исходящий документ.

**Базовые классы:** `Document`

**Поля:**
- `recipient: str` - получатель (по умолчанию "")

**Методы:**
- `approve() -> None` - согласовывает документ (переопределен)

**Декоратор:** `@dataclass`

---

### Класс `ContractDocument`

**Описание:** Договор.

**Базовые классы:** `Document`

**Поля:**
- `effective_from: datetime | None` - дата начала действия
- `effective_to: datetime | None` - дата окончания действия
- `total_amount: int` - сумма договора (по умолчанию 0)

**Методы:**
- `validate() -> None` - валидирует договор (проверяет корректность дат)

**Декоратор:** `@dataclass`

**Исключения:**
- `ValueError` - если дата окончания раньше даты начала

---

### Класс `InvoiceDocument`

**Описание:** Счет на оплату.

**Базовые классы:** `Document`

**Поля:**
- `amount_due: int` - сумма к оплате (по умолчанию 0)
- `due_date: datetime | None` - срок оплаты
- `paid: bool` - признак оплаты (по умолчанию False)

**Методы:**
- `mark_paid() -> None` - помечает счет как оплаченный

**Декоратор:** `@dataclass`

---

### Класс `OrderDocument`

**Описание:** Заказ.

**Базовые классы:** `Document`

**Поля:**
- `items: List[str]` - список позиций заказа

**Декоратор:** `@dataclass`

---

### Класс `DocumentRegistry`

**Описание:** Реестр номеров документов для предотвращения дублирования.

**Поля:**
- `numbers: set[str]` - множество зарегистрированных номеров

**Методы:**
- `register(number: str) -> None` - регистрирует номер документа
- `contains(number: str) -> bool` - проверяет, зарегистрирован ли номер

**Декоратор:** `@dataclass`

**Исключения:**
- `DuplicateDocumentError` - при попытке зарегистрировать уже существующий номер

---

## Модуль: workflow.py

### Класс `WorkflowState`

**Описание:** Константы для статусов документа.

**Константы:**
- `NEW` - новый документ
- `IN_REVIEW` - на согласовании
- `APPROVED` - согласован
- `REJECTED` - отклонен
- `ARCHIVED` - в архиве

---

### Класс `ApprovalStep`

**Описание:** Шаг согласования в маршруте.

**Поля:**
- `name: str` - название шага
- `role_name: str` - название роли для согласования
- `required: bool` - обязательность шага (по умолчанию True)
- `deadline_hours: int` - срок согласования в часах (по умолчанию 48)

**Методы:**
- `deadline(start: datetime) -> datetime` - вычисляет дедлайн от указанной даты

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `ApprovalRoute` (поле `steps`)
- Используется в классе `ApprovalTask` (поле `step`)

---

### Класс `ApprovalTask`

**Описание:** Задача согласования.

**Поля:**
- `step: ApprovalStep` - шаг согласования
- `assignee_id: str` - идентификатор исполнителя
- `created_at: datetime` - дата создания задачи
- `completed: bool` - признак завершения (по умолчанию False)
- `comment: str` - комментарий (по умолчанию "")

**Методы:**
- `complete(comment: str = "") -> None` - завершает задачу

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `ApprovalStep`

**Исключения:**
- `ApprovalStepError` - при попытке завершить уже завершенную задачу

---

### Класс `ApprovalRoute`

**Описание:** Маршрут согласования документа.

**Поля:**
- `name: str` - название маршрута
- `steps: List[ApprovalStep]` - список шагов согласования

**Методы:**
- `first_step() -> ApprovalStep` - возвращает первый шаг
- `next_step(current: ApprovalStep) -> Optional[ApprovalStep]` - возвращает следующий шаг

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит список объектов `ApprovalStep`
- Используется в классе `Document` (поле `approval_route`)

**Исключения:**
- `ApprovalStepError` - если маршрут пуст или шаг не принадлежит маршруту

---

### Класс `WorkflowTransition`

**Описание:** Переход между статусами в рабочем процессе.

**Поля:**
- `src: str` - исходный статус
- `dst: str` - целевой статус
- `reason: str` - причина перехода (по умолчанию "")

**Декоратор:** `@dataclass`

---

### Класс `Notification`

**Описание:** Уведомление пользователю.

**Поля:**
- `message: str` - текст сообщения
- `recipient_id: str` - идентификатор получателя
- `created_at: datetime` - дата создания

**Декоратор:** `@dataclass`

---

## Модуль: storage.py

### Класс `StorageLocation`

**Описание:** Расположение хранилища документов.

**Поля:**
- `name: str` - название хранилища
- `base_path: str` - базовый путь

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `DocumentStorage` (поле `location`)

---

### Класс `DocumentStorage`

**Описание:** Хранилище документов.

**Поля:**
- `location: StorageLocation` - расположение хранилища
- `quota: QuotaManager` - менеджер квот
- `_docs: Dict[str, Document]` - словарь документов
- `_attachments: Dict[str, DocumentAttachment]` - словарь вложений

**Методы:**
- `save(doc: Document) -> None` - сохраняет документ
- `get(number: str) -> Document` - получает документ по номеру
- `exists(number: str) -> bool` - проверяет существование документа
- `store_attachment(doc: Document, att: DocumentAttachment) -> None` - сохраняет вложение
- `archive(doc: Document) -> None` - архивирует документ

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `StorageLocation`
- Содержит ссылку на `QuotaManager`
- Хранит объекты `Document`
- Хранит объекты `DocumentAttachment`

**Исключения:**
- `DocumentNotFoundError` - если документ не найден

---

### Класс `ArchiveService`

**Описание:** Сервис для архивирования документов.

**Поля:**
- `storage: DocumentStorage` - хранилище документов

**Методы:**
- `archive_document(number: str) -> None` - архивирует документ
- `restore_document(number: str) -> None` - восстанавливает документ из архива

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `DocumentStorage`

---

## Модуль: security.py

### Класс `PasswordPolicy`

**Описание:** Политика паролей.

**Поля:**
- `min_length: int` - минимальная длина пароля (по умолчанию 8)
- `require_digit: bool` - требование наличия цифр (по умолчанию True)

**Методы:**
- `validate(password: str) -> bool` - проверяет пароль на соответствие политике

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `AuthService` (поле `policy`)

---

### Класс `Session`

**Описание:** Сессия пользователя.

**Поля:**
- `user_id: str` - идентификатор пользователя
- `token: str` - токен сессии
- `created_at: datetime` - дата создания
- `expires_at: datetime` - дата истечения

**Методы:**
- `is_active(moment: datetime) -> bool` - проверяет, активна ли сессия

**Декоратор:** `@dataclass`

---

### Класс `Token`

**Описание:** Токен аутентификации.

**Поля:**
- `value: str` - значение токена
- `issued_at: datetime` - дата выдачи

**Методы (статические):**
- `generate() -> Token` - генерирует новый токен

**Декоратор:** `@dataclass`

**Ассоциации:**
- Возвращается методом `AuthService.login`

---

### Класс `QuotaManager`

**Описание:** Менеджер квот хранилища.

**Поля:**
- `max_bytes: int` - максимальный размер в байтах
- `used_bytes: int` - использовано байтов (по умолчанию 0)

**Методы:**
- `can_allocate(size: int) -> bool` - проверяет, можно ли выделить указанный размер
- `allocate(size: int) -> None` - выделяет место

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `DocumentStorage` (поле `quota`)

**Исключения:**
- `StorageLimitExceededError` - при превышении квоты

---

## Модуль: payments.py

### Класс `Currency`

**Описание:** Константы валют.

**Константы:**
- `BYN` - белорусский рубль
- `USD` - доллар США
- `EUR` - евро

---

### Класс `Account`

**Описание:** Банковский счет.

**Поля:**
- `number: str` - номер счета
- `currency: str` - валюта (по умолчанию BYN)
- `balance: int` - баланс (по умолчанию 0)

**Методы:**
- `debit(amount: int) -> None` - списывает средства со счета
- `credit(amount: int) -> None` - зачисляет средства на счет

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `InMemoryPaymentProcessor` (поле `accounts`)

**Исключения:**
- `PaymentOperationError` - при отрицательной сумме или недостатке средств

---

### Класс `Transaction`

**Описание:** Финансовая транзакция.

**Поля:**
- `id: str` - идентификатор транзакции
- `src: str` - счет отправителя
- `dst: str` - счет получателя
- `amount: int` - сумма
- `created_at: datetime` - дата создания

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `InMemoryPaymentProcessor` (поле `transactions`)

---

### Класс `PaymentOrder`

**Описание:** Платежное поручение.

**Поля:**
- `invoice_number: str` - номер счета
- `src_account: str` - счет отправителя
- `dst_account: str` - счет получателя
- `amount: int` - сумма
- `currency: str` - валюта (по умолчанию BYN)

**Декоратор:** `@dataclass`

---

### Класс `BalanceChecker`

**Описание:** Проверка балансов и валют.

**Методы:**
- `ensure_same_currency(a: Account, b: Account) -> None` - проверяет совпадение валют

**Декоратор:** `@dataclass`

**Исключения:**
- `PaymentOperationError` - при несовпадении валют

---

### Класс `InMemoryPaymentProcessor`

**Описание:** Процессор платежей в памяти (реализация протокола PaymentProcessorProtocol).

**Поля:**
- `accounts: Dict[str, Account]` - словарь счетов
- `balance_checker: BalanceChecker` - проверка балансов
- `transactions: List[Transaction]` - список транзакций

**Методы:**
- `transfer(src_account: str, dst_account: str, amount: int) -> str` - выполняет перевод

**Декоратор:** `@dataclass`

**Ассоциации:**
- Хранит объекты `Account`
- Содержит ссылку на `BalanceChecker`
- Хранит список объектов `Transaction`
- Используется в классе `PaymentService` (поле `processor`)

---

## Модуль: services.py

### Класс `InMemoryDocumentRepository`

**Описание:** Репозиторий документов в памяти (реализация протокола DocumentRepositoryProtocol).

**Поля:**
- `storage: DocumentStorage` - хранилище документов

**Методы:**
- `save(doc: Document) -> None` - сохраняет документ
- `get(number: str) -> Document | None` - получает документ
- `exists(number: str) -> bool` - проверяет существование
- `search(query: str) -> Iterable[Document]` - поиск документов

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `DocumentStorage`
- Используется в классе `DocumentService` (поле `repo`)

---

### Класс `ConsoleNotifier`

**Описание:** Отправка уведомлений в консоль (реализация протокола NotifierProtocol).

**Методы:**
- `notify(message: str) -> None` - выводит уведомление в консоль

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `NotificationService` (поле `notifier`)

---

### Класс `SearchService`

**Описание:** Сервис поиска документов.

**Поля:**
- `repo: DocumentRepositoryProtocol` - репозиторий документов

**Методы:**
- `find(query: str) -> List[Document]` - ищет документы по запросу

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `DocumentRepositoryProtocol`

---

### Класс `ValidationService`

**Описание:** Сервис валидации документов.

**Методы:**
- `validate(doc: Document) -> None` - валидирует документ

**Декоратор:** `@dataclass`

**Ассоциации:**
- Используется в классе `DocumentService` (поле `validator`)

---

### Класс `NotificationService`

**Описание:** Сервис отправки уведомлений.

**Поля:**
- `notifier: NotifierProtocol` - провайдер уведомлений

**Методы:**
- `send(message: str) -> None` - отправляет уведомление

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `NotifierProtocol`
- Используется в классах `DocumentService`, `ApprovalService`, `PaymentService`

---

### Класс `DocumentService`

**Описание:** Основной сервис работы с документами.

**Поля:**
- `repo: DocumentRepositoryProtocol` - репозиторий документов
- `registry: DocumentRegistry` - реестр номеров
- `validator: ValidationService` - сервис валидации
- `notifier: NotificationService` - сервис уведомлений

**Методы:**
- `register(doc: Document) -> None` - регистрирует новый документ
- `add_attachment(number: str, att: DocumentAttachment) -> None` - добавляет вложение
- `send_for_approval(number: str, route: ApprovalRoute) -> None` - отправляет на согласование
- `sign(number: str, user: User) -> None` - подписывает документ
- `archive(number: str) -> None` - архивирует документ
- `require(number: str) -> Document` - получает документ или выбрасывает исключение

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылки на `DocumentRepositoryProtocol`, `DocumentRegistry`, `ValidationService`, `NotificationService`

**Исключения:**
- `DocumentNotFoundError` - если документ не найден
- `AccessDeniedError` - если пользователь заблокирован

---

### Класс `ApprovalService`

**Описание:** Сервис согласования документов.

**Поля:**
- `notifier: NotificationService` - сервис уведомлений

**Методы:**
- `route_for_role(role_name: str) -> ApprovalRoute` - создает маршрут для роли
- `approve(doc: Document) -> None` - согласовывает документ

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `NotificationService`

---

### Класс `PaymentService`

**Описание:** Сервис платежей.

**Поля:**
- `processor: PaymentProcessorProtocol` - процессор платежей
- `notifier: NotificationService` - сервис уведомлений

**Методы:**
- `pay_invoice(invoice: InvoiceDocument, src: str, dst: str, amount: int) -> str` - оплачивает счет

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылки на `PaymentProcessorProtocol` и `NotificationService`

---

### Класс `AuthService`

**Описание:** Сервис аутентификации.

**Поля:**
- `users: Dict[str, str]` - словарь пользователей (логин: пароль)
- `policy: PasswordPolicy` - политика паролей

**Методы:**
- `login(login: str, password: str) -> Token` - выполняет вход и возвращает токен

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылку на `PasswordPolicy`

**Исключения:**
- `AuthFailedError` - при неудачной аутентификации

---

## Модуль: cli.py

### Класс `DemoContext`

**Описание:** Контекст для демонстрационного приложения.

**Поля:**
- `repo: InMemoryDocumentRepository` - репозиторий
- `storage: DocumentStorage` - хранилище
- `doc_service: DocumentService` - сервис документов
- `appr_service: ApprovalService` - сервис согласования
- `pay_service: PaymentService` - сервис платежей
- `auth: AuthService` - сервис аутентификации

**Декоратор:** `@dataclass`

**Ассоциации:**
- Содержит ссылки на все основные сервисы системы

---

## Модуль: exceptions.py

### Список исключений

1. **`DocumentNotFoundError`** - документ не найден
2. **`InvalidDocumentStatusError`** - недопустимая операция для текущего статуса документа
3. **`AccessDeniedError`** - доступ запрещен
4. **`ApprovalStepError`** - ошибка в процессе согласования
5. **`InvalidSignatureError`** - недопустимая операция подписания
6. **`UserBlockedError`** - пользователь заблокирован
7. **`VersionConflictError`** - конфликт версий/блокировок документа
8. **`RouteNotFoundError`** - маршрут не найден
9. **`PaymentOperationError`** - ошибка платежной операции
10. **`AuthFailedError`** - ошибка аутентификации
11. **`DuplicateDocumentError`** - дублирование номера документа
12. **`StorageLimitExceededError`** - превышен лимит хранилища

Все исключения наследуются от базового класса `Exception`.

---

## Основные ассоциации между классами

1. **User ← → Role** (многие ко многим) - пользователь имеет несколько ролей
2. **Role ← → Permission** (один ко многим) - роль содержит разрешения
3. **User → Organization** (многие к одному) - пользователь принадлежит организации
4. **User → Department** (многие к одному) - пользователь относится к отделу
5. **Document → User** (многие к одному) - документ имеет автора
6. **Document → Organization** (многие к одному) - документ принадлежит организации
7. **Document → Department** (многие к одному) - документ относится к отделу
8. **Document ← → DocumentVersion** (один ко многим) - документ имеет версии
9. **Document ← → DocumentAttachment** (один ко многим) - документ имеет вложения
10. **Document ← → Signature** (один ко многим) - документ имеет подписи
11. **Document → ApprovalRoute** (многие к одному) - документ имеет маршрут согласования
12. **Document → DocumentMetadata** (один к одному) - документ имеет метаданные
13. **Document → DocumentLock** (один к одному) - документ имеет блокировку
14. **ApprovalRoute ← → ApprovalStep** (один ко многим) - маршрут содержит шаги
15. **ApprovalTask → ApprovalStep** (многие к одному) - задача связана с шагом
16. **DocumentStorage → StorageLocation** (многие к одному) - хранилище имеет расположение
17. **DocumentStorage → QuotaManager** (один к одному) - хранилище использует квоты
18. **InMemoryPaymentProcessor ← → Account** (один ко многим) - процессор управляет счетами
19. **InMemoryPaymentProcessor ← → Transaction** (один ко многим) - процессор хранит транзакции
20. **InMemoryPaymentProcessor → BalanceChecker** (многие к одному) - процессор использует проверку
21. **DocumentService → DocumentRepositoryProtocol** - сервис использует репозиторий
22. **DocumentService → DocumentRegistry** - сервис использует реестр
23. **DocumentService → ValidationService** - сервис использует валидацию
24. **DocumentService → NotificationService** - сервис использует уведомления
25. **ApprovalService → NotificationService** - сервис согласования использует уведомления
26. **PaymentService → PaymentProcessorProtocol** - сервис платежей использует процессор
27. **PaymentService → NotificationService** - сервис платежей использует уведомления
28. **AuthService → PasswordPolicy** - сервис аутентификации использует политику
29. **InMemoryDocumentRepository → DocumentStorage** - репозиторий использует хранилище
30. **NotificationService → NotifierProtocol** - сервис уведомлений использует провайдер

---

## Замечания и рекомендации

### Положительные аспекты:

1. ✅ **Хорошая архитектура** - использование протоколов (Protocol) для абстракции интерфейсов
2. ✅ **Dataclasses** - правильное использование `@dataclass` для упрощения кода
3. ✅ **Разделение ответственности** - четкое разделение на модули по функциональности
4. ✅ **Типизация** - использование type hints для улучшения читаемости и проверки типов
5. ✅ **Обработка исключений** - продуманная система исключений для разных ситуаций
6. ✅ **Паттерны проектирования** - использование Service Layer, Repository, Protocol/Interface
7. ✅ **Документация кода** - код хорошо структурирован и понятен

### Замечания и предложения по улучшению:

1. ⚠️ **Хранение паролей в открытом виде** 
   - **Проблема:** В `AuthService` пароли хранятся в открытом виде в словаре
   - **Рекомендация:** Использовать хеширование паролей (bcrypt, argon2)

2. ⚠️ **Отсутствие логирования**
   - **Проблема:** Нет системы логирования операций
   - **Рекомендация:** Добавить логирование критических операций (создание документов, платежи, изменение статусов)

3. ⚠️ **Отсутствие тестов**
   - **Проблема:** В репозитории есть директория tests/, но pytest не установлен
   - **Рекомендация:** Добавить файл requirements.txt с зависимостями и документировать запуск тестов

4. ⚠️ **Безопасность токенов**
   - **Проблема:** Токены генерируются, но нет механизма их проверки и инвалидации
   - **Рекомендация:** Добавить TokenManager для управления жизненным циклом токенов

5. ⚠️ **Отсутствие персистентности**
   - **Проблема:** Все данные хранятся в памяти и теряются при перезапуске
   - **Рекомендация:** Добавить реализацию с сохранением в БД или файловой системе

6. ⚠️ **Недостаточная валидация ввода**
   - **Проблема:** Некоторые методы не проверяют корректность входных данных
   - **Рекомендация:** Добавить валидацию во все публичные методы сервисов

7. ⚠️ **Использование `datetime.utcnow()`**
   - **Проблема:** `datetime.utcnow()` deprecated в Python 3.12+
   - **Рекомендация:** Заменить на `datetime.now(timezone.utc)`

8. ⚠️ **Отсутствие пагинации**
   - **Проблема:** Метод `search()` может вернуть слишком много результатов
   - **Рекомендация:** Добавить пагинацию для методов поиска

9. ⚠️ **Транзакционность операций**
   - **Проблема:** Отсутствует транзакционность при работе с несколькими объектами
   - **Рекомендация:** Добавить Unit of Work паттерн

10. ⚠️ **Документация API**
    - **Проблема:** Отсутствуют docstrings в большинстве методов
    - **Рекомендация:** Добавить подробные docstrings с описанием параметров и возвращаемых значений

### Критические замечания:

1. 🔴 **Хранение паролей без хеширования** - НЕОБХОДИМО исправить перед использованием в продакшене
2. 🔴 **Отсутствие авторизации** - Есть аутентификация, но нет проверки прав доступа к операциям
3. 🔴 **Отсутствие аудита** - Нет записи истории изменений для критических операций

---

## Запуск демо-приложения

```bash
cd /home/runner/work/PPOIS/PPOIS/lab2
python main.py
```

Демо-приложение демонстрирует:
- Создание документов (входящий документ и счет)
- Регистрацию документов в системе
- Отправку на согласование
- Согласование документа
- Добавление версий и подписей
- Обработку исключений
- Добавление вложений
- Архивирование документов
- Проведение платежа по счету

---

## Заключение

Проект представляет собой хорошо спроектированную систему электронного документооборота с четкой архитектурой, использованием современных паттернов проектирования и типизации Python. Код чистый, структурированный и легко расширяемый.

Для использования в реальных условиях необходимо:
1. Исправить критические замечания по безопасности
2. Добавить персистентность данных
3. Реализовать полноценную систему логирования и аудита
4. Покрыть код тестами
5. Добавить документацию API

**Дата создания README:** 2025-11-14
